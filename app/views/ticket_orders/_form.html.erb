<%= form_with(model: ticket_order) do |form| %>
  <% if ticket_order.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket_order.errors.count, "error") %> prohibited this ticket_order from being saved:</h2>

      <ul>
        <% ticket_order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :concert_id, style: "display: block" %>
    <%= form.text_field :concert_id %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.label :count, style: "display: block" %>
    <%= form.number_field :count %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
