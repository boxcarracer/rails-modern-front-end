<%= form_with(model: vanue) do |form| %>
  <% if vanue.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vanue.errors.count, "error") %> prohibited this vanue from being saved:</h2>

      <ul>
        <% vanue.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :rows, style: "display: block" %>
    <%= form.number_field :rows %>
  </div>

  <div>
    <%= form.label :seats_per_row, style: "display: block" %>
    <%= form.number_field :seats_per_row %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
